// tslint:disable no-magic-numbers

import { Scalar, to } from '@musical-patterns/utilities'

const SUSTAIN_AMOUNT: Scalar<Scalar<Scalar>> = to.Scalar<Scalar<Scalar>>(0.9)

export {
    SUSTAIN_AMOUNT,
}
