// tslint:disable no-magic-numbers

import { as, Scalar } from '@musical-patterns/utilities'

const SUSTAIN_AMOUNT: Scalar<Scalar<Scalar>> = as.Scalar<Scalar<Scalar>>(0.9)

export {
    SUSTAIN_AMOUNT,
}
