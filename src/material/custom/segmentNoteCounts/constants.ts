// tslint:disable no-magic-numbers

import { Fraction, to } from '@musical-patterns/utilities'

const INITIAL_RATIO_FOR_COMPUTE_SEGMENT_RATIOS: Fraction = to.Fraction([ 1, 1 ])

export {
    INITIAL_RATIO_FOR_COMPUTE_SEGMENT_RATIOS,
}
